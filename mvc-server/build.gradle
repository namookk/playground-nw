bootJar {
    enabled = true
    archivesBaseName = 'mvc-server'
    archiveFileName = 'mvc-server.jar'
    archiveVersion = "0.0.1"
}
jar { enabled = false }

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}
